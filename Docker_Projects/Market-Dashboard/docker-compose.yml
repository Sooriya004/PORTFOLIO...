version: "3.9"

services:

  dashboard:
    build: .
    container_name: market-dashboard
    ports:
      - "5000:5000"
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: market-redis
